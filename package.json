{
  "name": "node_boiler_plate",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "node --watch app.js",
    "prisma:generate": "npm prisma generate",
    "prisma:migrate": "npm run prisma:generate && prisma migrate dev",
    "prisma:stud" : "npx prisma studio"
  },
  "private": true,
  "dependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0",
    "prisma": "^7.2.0"
  }
}
